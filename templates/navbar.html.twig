<!doctype html>
<html lang="fr">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>PetMatch - Sauvez une vie</title>
  <style>
  @import url('https://fonts.googleapis.com/css2?family=Montserrat:wght@500&display=swap');
        * { font-family: 'Montserrat', sans-serif; }
    .header-nav {
          display: flex;
          flex-direction: column;
          justify-content: center;
          align-items: center;
          position: relative;
          min-height: 50vh;
        }
        div {
          color: #727272;
          text-align: center;
        }
        .title {
          margin: 16px;
          font-size: 96px;
          color: #d8515e;
          text-transform: uppercase;
          font-weight: 600;
          transition: all 1s ease-in-out;
          position: relative;
        }
        .title::before {
          content: attr(data-item);
          transition: all 1s ease-in-out;
          color: #ccc;
          position: absolute;
          top: 0;
          bottom: 0;
          left: 0;
          right: 0;
          width: 0;
          overflow: hidden;
        }
        .title:hover::before {
          width: 100%;
        }
        section { width: 100%; }
        nav {
          margin: 10px;
          background: #f9f9f9;
          padding: 0;
        }
        nav .menuItems {
          list-style: none;
          display: flex;
          justify-content: center;
        }
        nav .menuItems li {
          margin: 50px;
          align-items: center;
          display: flex;
        }
        nav .menuItems li a {
          text-decoration: none;
          color: #8f8f8f;
          font-size: 16px;
          font-weight: 400;
          transition: all 0.5s ease-in-out;
          position: relative;
          text-transform: uppercase;
        }
        nav .menuItems li a::before {
          content: attr(data-item);
          transition: 0.5s;
          color: #d8515e;
          position: absolute;
          top: 0;
          bottom: 0;
          left: 0;
          right: 0;
          width: 0;
          overflow: hidden;
        }
        nav .menuItems li a:hover::before {
          width: 100%;
          transition: all 0.5s ease-in-out;
        }

        footer {
          margin-top: 70px;
          width: 100%;
          height: 60px;
          line-height: 60px;
          font-size: 14px;
          background-color: #f1f1f1;
          color: #000;
          text-align: center;
        }
        footer a {
          text-decoration: none;
          color: inherit;
          border-bottom: 1px solid;
        }
        footer a:hover {
          border-bottom: 1px transparent;
        }
        .icone {
          width: 40px;
        }
        .profil{ display: flex; justify-content: center; align-items: center; }
        .profil img { margin-left: 10px; }
        .profil p { font-weight: 600; color: black; }
  </style>
</head>

<div class="header-nav">
<p class="title" data-item='PetMatch'>PetMatch</p>

<section>
  <nav>
    <ul class="menuItems">
      <li><a href='/' data-item='Accueil'>Accueil</a></li>
      <li><a href='/repertoire' data-item='Répertoire'>Répertoire</a></li>
      <li><a href='/adoption' data-item='Mettre.en.adoption'>Mettre en adoption</a></li>
      {% if connexion.login %}
        <li><a href="/account" class='profil'><p>{{ connexion.login }}</p><img class="icone" src="https://cdn-icons-png.flaticon.com/256/9449/9449194.png"/></a></li>
    {% else %}
        <li><a href="/register"><img class="icone" src="https://cdn-icons-png.flaticon.com/512/5509/5509636.png"/></a></li>
    {% endif %}


    </ul>
  </nav>

</section>
</div>
<body>
  {% block body %}{% endblock %}

  <footer>
    <div class="footer-copyright text-center">
      &copy; Developed with ❤️</i> by
      <a href="#" class="white-text">PetMatch</a>.
      <a href="#">Projet PHP 2023</a>
    </div>
  </footer>
</body>
</html>
